BIN_DIR="$(cd `dirname $BASH_SOURCE` && pwd)"
PROJ_DIR="$(dirname $BIN_DIR)"
echo Setting up Web Notes environment in $PROJ_DIR

source $PROJ_DIR/env/bin/activate
export PYTHONPATH=$PROJ_DIR
export PATH=$PROJ_DIR/bin:$PATH
