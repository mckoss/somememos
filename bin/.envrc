export PROJECT=somememos
export BIN_DIR="$(cd `dirname $BASH_SOURCE` && pwd)"
export PROJ_DIR="$(dirname $BIN_DIR)"
export DOWN_DIR="$HOME/Downloads"
export ENV_DIR="$PROJ_DIR/env"
export DATA_DIR="$HOME/.$PROJECT"
